	.CSEG
	.ORG	0x3E000 / 2

; =============================================================================
; ===
; =============================================================================

bls_Reset:
	CLI

	LDI		R17, HIGH( RAMEND )
	LDI		R16, LOW( RAMEND )
	OUT		SPH, R17
	OUT		SPL, R16

	JMP		0x000000
